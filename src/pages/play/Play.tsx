import GameBoard from 'components/GameBoard/GameBoard';
import Layout from 'components/layout/Layout';

const Play = () => {
  return (
    <Layout>
      <GameBoard />
    </Layout>
  );
};

export default Play;
